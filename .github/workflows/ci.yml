name: CI Builds

on:
  push:
    branches: ["*"]
  pull_request:
    branches: ["*"]

jobs:
  sanity_check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run bash command
        run: echo hello